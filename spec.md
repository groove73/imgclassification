# 사람 이미지 분류기

파이선을 이용한 사람 이미지 분류기를 기계학습(ML)을 이용하여 만든다.

**Goal**: 사람 이미지를 학습시킨 후 사람의 이미지를 업로드하고 유사한 사람을 추천해준다.

## Context

이 프로젝트는 파이썬을 이용한 사람 이미지 분류기를 기계학습(ML)을 이용하여 만들고, 이를 웹을 통해서 서비스를 한다.

- **ML**: 연예인의 이미를 이용해서 유사도를 추출 내가 업로드한 사진과의 유사도가 가장 높은 것을 추천한다.
- **Web**: 첨부파일이나 카메라 모듈을 통해서 이미지를 첨부 ML이 판단할 수 있게 전달하는 역할을 한다.

## Core Features

- ML은 CNN 모델중 가장 성능이 좋은 것을 기반으로 만든다.
- 웹은 FAST API를 이용해서 처리한다.

### 1. Processing Logic

- **학습**: 인터넷에서 유명 사진 데이터를 가지고 학습 할 수 있게 전처리된 것을 찾아서 학습니다.
- **추천**: CNN 모델을 이용해서 유사도를 추출 내가 업로드한 사진과의 유사도가 가장 높은 것을 추천한다.

### 2. Architecture Constraints

- **ML**: 파이선을 이용해서 가장 최신 모델을 기반으로 학습니다.
- **Data**: <http://www.cs.columbia.edu/CAVE/databases/pubfig/download/dev_urls.txt>) 활용
- **WEB**: Fast API를 이용해서 클린 아키텍처로 처리한다.
